aws:
  dynamodb:
    endpoint: http://localhost:8000
    region: us-east-1
    access-key: local
    secret-key: local
    table-name: tasks-test

logging:
  level:
    com.taskmanagent: INFO
    software.amazon.awssdk: WARN
